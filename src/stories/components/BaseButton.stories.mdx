import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import { withKnobs, object, boolean, text, select } from '@storybook/addon-knobs';
import { withInfo } from 'storybook-addon-vue-info'
import BaseButton from '../../components/BaseButton.vue';

<Meta title="Form fields/Button" component={BaseButton} />

# Button

A multi-functional button that handles external links, internal links and generic button clicks

<details>
<summary>
Todo list
</summary>

- Support Vue router

</details>

<Props of={BaseButton} />

# Defaults

This is a simple button with some text in it.

<Preview>
  <Story name="Defaults">
    {{
      components: {
        BaseButton
      },
      template: `
        <div class="py-16 flex justify-center">
          <BaseButton @click="action">Hello Button</BaseButton>
        </div>`,
        methods: { action: action("clicked") }
    }}
  </Story>
</Preview>

# States and sizes

This is a button modified using the Colour and Variant props.

<Preview>
  <Story name="States and sizes" decorators={[withKnobs()]}>
    {{
      components: {
        BaseButton
      },
      props: {
        colour: {
          default: select('Colour', ['primary', 'secondary'], 'primary')
        },
        variant: {
          default: select('Variant', ['large', 'small', 'round'], 'large')
        },
        disabled: {
          default: select('Disabled', [true, false], false)
        },
      },
      template: `
        <div class="py-16 flex justify-center">
          <base-button :colour="colour" :variant="variant" :disabled="disabled">
            Bigger, amazing
          </base-button>
        </div>`,
        methods: { action: action("clicked") }
    }}
  </Story>
</Preview>
