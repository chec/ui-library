import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { BaseButton } from '../../index';

<Meta title="Directives/Tooltips" />

# Default:

<Preview>
  <Story name="Default">
    {{
      components: {
        BaseButton,
      },
      template: `
      <div class="flex relative w-full mx-auto max-w-lg m-10">
        <BaseButton v-tooltip="{
            content: 'I am the automatic position for a tooltip',
            show: true,
            autoHide: false,
        }">Default</BaseButton>
        <BaseButton v-tooltip.left="'On the left of the target'">Left</BaseButton>
        <BaseButton v-tooltip.right="'Positioned on the right of the BaseButton'">Right</BaseButton>
        <BaseButton v-tooltip.top="'Above the target'">Top</BaseButton>
        <BaseButton v-tooltip.bottom="'Popover is shown below the target'">Bottom</BaseButton>
      </div>`
    }}
  </Story>
</Preview>
