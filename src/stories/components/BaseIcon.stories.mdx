import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import { withKnobs, object, boolean, text, select } from '@storybook/addon-knobs';
import { withInfo } from 'storybook-addon-vue-info'
import BaseIcon from '../../components/BaseIcon.vue';

<Meta title="Components/Icon" component={BaseIcon} />

# Icon

<Props of={ BaseIcon } />

# Basic Icons

<Preview>
  <Story name="Basic">
    {{
      components: {
        BaseIcon
      },
      props: {
        icon: {
          default: select('Icon', ['left', 'right', 'close','check'], 'left')
        },
        customClass: {
          default: text('Custom classes', 'text-primary-blue')
        }
      },
      template: `
        <div class="py-16 flex justify-center">
          <BaseIcon :icon="icon" class="w-10 h-10" :class="customClass" />
        </div>`,
    }}
  </Story>
</Preview>

