import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import { text, select, boolean } from '@storybook/addon-knobs';
import BaseTab from '../../components/BaseTab.vue';

<Meta title="Components/Tab" component={BaseTab} />

# Tab

<Props of={BaseTab} />

<Preview>
  <Story name="Default">
    {{
      components: {
        BaseTab
      },
      props: {
        disabled: {
          default: boolean('Disable Button', false)
        },
        active: {
          default: boolean('Set as Active', false)
        },
      },
      methods: {
        click(tab) {
          action(`Clicked tab "${tab}"`)();
        }
      },
      template: `
        <div class="flex justify-center font-lato mx-auto max-w-lg py-6 bg-gray-100">
          <BaseTab
            :disabled="disabled"
            :active="active"
            @click="click('Tab Label')""
          >
            Tab Label
          </BaseTab>
        </div>`
    }}
  </Story>
</Preview>
