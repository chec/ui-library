import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import { withKnobs, object, boolean, text, select } from '@storybook/addon-knobs';
import { withInfo } from 'storybook-addon-vue-info'
import ChecIcon from '../../components/ChecIcon.vue';
import icons from '../../lib/icons';

<Meta title="Components/Icon" component={ChecIcon} />

<details>
<summary>
Todo list
</summary>
</details>

# Icon

#### There are  main groups of icons: UI, Navigation, Marketing
- UI icons are standard stroke icons utilized throughout product interface
    - Sizes are customized using inline class attributes `w-3 h-3` and `w-4 h-4`

<Props of={ ChecIcon } />

## Basic Stroke Icon

<Preview>
  <Story name="UI">
    {{
      components: {
        ChecIcon
      },
      props: {
        icon: {
          default: select('Icon', Object.keys(icons), 'left')
        },
        color: {
          default: select('Color', ['text-gray-500', 'text-white', 'text-red-600', 'text-green-600'], 'text-gray-500')
        },
      },
      template: `
        <div class="py-16 flex justify-center bg-gray-200">
          <chec-icon :icon="icon" class="w-4 h-4" :class="color" />
        </div>`,
    }}
  </Story>
</Preview>

## Basic Icons List

<Preview>
  <Story name="UI icons list">
    {{
      components: {
        ChecIcon
      },
      props: {
        icons: {
          default: icons,
        },
        color: {
          default: select('Color', ['text-gray-500', 'text-white', 'text-red-600', 'text-green-600'], 'text-gray-500')
        },
      },
      template: `
        <div class="py-16 flex justify-center bg-gray-200">
          <chec-icon v-for="(_, icon) in icons" :icon="icon" class="w-4 h-4" :class="color" v-tooltip.top="icon"/>
        </div>`,
    }}
  </Story>
</Preview>
