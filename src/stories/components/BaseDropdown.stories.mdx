import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import BaseDropdown from '../../components/BaseDropdown.vue';
import BaseOption from '../../components/BaseOption.vue';
const defaultOptions = [
  { value: '', label: 'Select an option' },
  { value: 'optionKey1', label: 'Option Value 2' },
  { value: 'optionKey2', label: 'Option Value 3' }
];

<Meta title="Components/BaseDropdown" component={BaseDropdown} />

# Dropdown

<Props of={BaseDropdown} />

# Default:

<Preview>
  <Story name="Default">
    {{
      components: {
        BaseDropdown,
        BaseOption,
      },
      data() {
        return {
          option: "",
          options: defaultOptions,
        }
      },
      template: `
        <div class="p-16 flex justify-center w-full bg-gray-100">
          <BaseDropdown
            class="max-w-md"
            v-model="option"
            :options="options"
            />
        </div>`
    }}
  </Story>
</Preview>

# With Placeholder:

<Preview>
  <Story name="With Placeholder">
    {{
      components: {
        BaseDropdown,
        BaseOption,
      },
      data() {
        return {
          option: "",
          options: defaultOptions,
        }
      },
      template: `
        <div class="p-16 flex justify-center w-full bg-gray-100">
          <BaseDropdown
            class="max-w-md"
            placeholder="Example Dropdown Placeholder"
            v-model="option"
            :options="options"
            />
        </div>`
    }}
  </Story>
</Preview>

# In context:

<Preview>
  <Story name="In context">
    {{
      components: {
        BaseDropdown,
        BaseOption,
      },
      data() {
        return {
          option: "",
          options: defaultOptions,
        }
      },
      template: `
        <div class="p-16 bg-gray-100 text-center">
          <div>Some content</div>
          <BaseDropdown
            class="max-w-md mx-auto"
            placeholder="Example Dropdown Placeholder"
            v-model="option"
            :options="options"
            baseOptionClass="text-left"
            />
            <div>Some more content</div>
        </div>`
    }}
  </Story>
</Preview>
