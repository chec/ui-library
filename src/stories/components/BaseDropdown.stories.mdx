import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import BaseDropdown from '../../components/BaseDropdown.vue';
import BaseDropdownOption from '../../components/BaseDropdownOption.vue';

<Meta title="Components/BaseDropdown" component={BaseDropdown} />

# dropdown

<Props of={BaseDropdown} />

# Default:

<Preview>
  <Story name="Default">
    {{
      components: {
        BaseDropdown,
        BaseDropdownOption,
      },
      data() {
        return {
          option: "",
          options: [{value: '', label: 'Select an option'}, { value: 'optionKey1', label: 'Option Value 3'}, {value: 'optionKey2', label: 'Option Value 2'}],
        }
      },
      methods: {
        onOptionSelect: action('option-selected'),
      },
      template: `
        <div class="p-16 flex justify-center w-full bg-gray-100">
          <BaseDropdown
            class="max-w-2xl"
            placeholder="Example Dropdown"
            v-model="option"
            :options="options"
            />
        </div>`
    }}
  </Story>
</Preview>
