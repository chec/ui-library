import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import BaseDropdown from '../../components/BaseDropdown.vue';

<Meta title="Components/BaseDropdown" component={BaseDropdown} />

# dropdown

<Props of={BaseDropdown} />

# Default:

<Preview>
  <Story name="Default">
    {{
      components: {
        BaseDropdown
      },
      data() {
        return {
          option: "optionKey1",
        }
      },
      methods: {
        onOptionSelect: action('option-selected'),
      },
      template: `
        <div class="p-16 flex justify-center w-full bg-gray-100">
          <BaseDropdown class="max-w-2xl" v-model="option" @input="onOptionSelect" :options="[{value: 'optionKey1', label: 'Option Value 1'}, {value: 'optionKey2', label: 'Option Value 2'}]"/>
        </div>`
    }}
  </Story>
</Preview>
