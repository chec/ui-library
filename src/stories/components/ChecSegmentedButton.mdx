import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from "@storybook/addon-actions";
import { text, select, boolean } from '@storybook/addon-knobs';
import ChecSegmentedButton from '../../components/ChecSegmentedButton.vue';

<Meta title="Components/Segmented Buttons" component={ChecSegmentedButton} />

# Segmented Buttons

<Props of={ChecSegmentedButton} />

<Preview>
  <Story name="Default">
    {{
      components: {
        ChecSegmentedButton,
      },
      props: {
        active: {
          default: boolean('Active', false)
        },
      },
      methods: {
        click(btn) {
          action(`Selected button "${btn}"`)();
        },
      },
      template: `
        <div class="flex justify-center font-lato uppercase mx-auto max-w-lg py-6 bg-gray-100">
          <ChecSegmentedButton
            :active="active"
            @click="click('Label')"
          >
            Label
          </ChecSegmentedButton>
        </div>`
    }}
  </Story>
</Preview>
